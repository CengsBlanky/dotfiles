function music
    # offer function to play/pause/stop mpc music
    # chose from play list or single music by fzf
    # privide options based on params: play(single music and playlist), pause, stop
    # run without param to toggle play and pause
end
