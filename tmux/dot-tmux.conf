# setting the prefix from C-b to C-a
set -g prefix C-Space
unbind C-b
# use key binding to reload configuaration file
bind r source-file ~/.tmux.conf \; display "Reloaded!"
# splitting panes with | and -
bind | split-window -h
bind - split-window -v
# enable mouse on globally
set -g mouse on
# set vi as default editor
set -g status-keys vi
# navigating panes
bind -n M-Left select-pane -L
bind -n M-Down select-pane -D
bind -n M-Up select-pane -U
bind -n M-Right select-pane -R
# switch between windows
bind -n C-Right next-window
bind-key -T prefix 0 select-window -t :0
# start window and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
bind -n M-1 select-window -t :1
bind -n M-2 select-window -t :2
bind -n M-3 select-window -t :3
bind -n M-4 select-window -t :4
bind -n M-5 select-window -t :5
bind -n M-6 select-window -t :6
bind -n M-7 select-window -t :7
bind -n M-8 select-window -t :8
bind -n M-9 select-window -t :9
bind-key -T prefix k kill-window
bind-key -T prefix x kill-pane
unbind-key -T root Escape
unbind-key -T root WheelUpStatus
# use vim as copy mode editor
setw -g mode-keys vi
# use system clipboard instead
unbind -T copy-mode-vi Space;
unbind -T copy-mode-vi Enter;

bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"

# Improve colors
# set -g default-terminal 'screen-256color'
# set -g default-terminal 'tmux-256color'

set-option -sg escape-time 10
set-option -g focus-events on
# set-option -sa terminal-overrides ',alacritty:RGB'
set-option -g default-shell /usr/local/bin/fish
# {n}vim compability
# set -g default-terminal "screen-256color"
set -g default-terminal "xterm-256color"
# make status bar look better

set-option -g status-right "#{?window_bigger,[#{window_offset_x}#,#{window_offset_y}] ,}\"#{=21:pane_title}\" %H:%M %b-%d"

# default statusbar colors
set-option -g status-style fg=colour7,bg=colour237 #yellow and base02

# default window title colors
# set-window-option -g window-status-style fg=colour94,bg=default #base0 and default
set-window-option -g window-status-style fg=colour247

# active window title colors
set-window-option -g window-status-current-style fg=colour15,bg=default #orange and default
#set-window-option -g window-status-current-style bright

# pane border
set-option -g pane-border-style fg=black #base02
set-option -g pane-active-border-style fg=brightgreen #base01

# message text
set-option -g message-style fg=brightred,bg=black #orange and base01

# pane number display
set-option -g display-panes-active-colour blue #blue
set-option -g display-panes-colour brightred #orange

# clock
set-window-option -g clock-mode-colour green #green

# bell
set-window-option -g window-status-bell-style fg=black,bg=red #base02, red
