#!/bin/bash
set -b
# set timeout to 0(never) and disable screen saver
xset dpms 0 0 0
xset s off
xrdb -merge ~/.Xresources
xmodmap ~/.Xmodmap
picom --daemon
# custom bin path
export PATH="$HOME/.local/share/flatpak/exports/bin:$PATH"
export PATH="$HOME/.local/share/jetbrains_toolbox/bin:$PATH"
export PATH="$HOME/.local/bin:$PATH"
# fcitx5 env
export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx
export SDL_IM_MODULE=fcitx
# for dunst running by user systemd
systemctl --user import-environment DISPLAY XAUTHORITY
fcitx5 -d --verbose 3
flameshot &
feh --bg-fill ~/Pictures/wallpaper/defaultwall.jpg &
unclutter --ignore-buttons 4,5,6,7 --fork
status_right.sh &
exec dwm
